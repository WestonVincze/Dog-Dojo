@model DogListViewModel

@{
  ViewData["Title"] = Model.CurrentBreed + 's';
}

<h1>@(Model.CurrentBreed + 's')</h1>

<div class="row">
  @foreach (var dog in Model.Dogs)
  {
    <partial name="_DogCard" model="dog" />
  }
</div>